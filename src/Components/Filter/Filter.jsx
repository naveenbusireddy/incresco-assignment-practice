import React from "react";

const Filter = (props) => {
    return(
        <>
            {/* {props.selectedFilter && props.selectedFilter.map((option) => 
            <li>
                {option.id}
            </li>)} */}

            <h1>Hello Welcome</h1>
        </>
    )
}

export default Filter;